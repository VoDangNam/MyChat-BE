spring.application.name=My_Chat
server.port=8081

#spring.datasource.url=jdbc:mysql://localhost:3306/mychat
#spring.datasource.username=root
#spring.datasource.password=123456
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#
jwt.secret= mychat_secret_key_123456789_mychat_secret_key
# ========== DATABASE ==========
# ========== DATABASE ==========
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ========== CORS (cho phép React FE truy c?p) ==========
# Cho phép g?i API t? React (ví d?: https://mychat-fe.onrender.com)
spring.web.cors.allowed-origins=${FRONTEND_URL}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
